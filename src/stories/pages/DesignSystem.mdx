import { Meta, Unstyled } from '@storybook/blocks'

import { Card } from '../../lib/components/Card'
import { CardHeader } from '../../lib/components/CardHeader'
import { CardContent } from '../../lib/components/CardContent'
import { Grid } from '../../lib/components/Grid'
import { Layer } from '../../lib/components/Layer'
import { Table } from '../../lib/components/Table'
import { TableHead } from '../../lib/components/TableHead'
import { TableCell } from '../../lib/components/TableCell'
import { TableRow } from '../../lib/components/TableRow'
import { Text } from '../../lib/components/Text'

import variables from '../../lib/styles/common/_variables.scss'

import { HeaderRow, HeadingRows, BodyRows } from './components/Typography'
import Colour from './components/Colours'

<Meta title="Design system" />

<div class="SBMarkdown">

# Design system

Here is an overview of the Shoomio design system and its different elements.

- [Colours](#colours)
- [Typography](#typography)
- [Spacing](#spacing)
- [Misc](#misc)

## Colours

Here is a presentation of the different colours used.

To learn how to override colour values with your own branding, see [How to use](?path=/docs/how-to-use--docs) page.

#### Common

<Unstyled>
  <Grid wrapper spacing={2}>
    <Colour background={variables.black} variable='black' />
    <Colour background={variables.white} variable='white' />
    <Grid item col={2}>
      <div style={{backgroundColor: variables.black, border: '1px solid', borderColor: variables.border, height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
      <Text variant='caption' weight='bold'>$black</Text>
    </Grid>
    <Grid item col={2}>
      <div style={{backgroundColor: variables.white, height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
      <Text variant='caption' weight='bold'>$white</Text>
    </Grid>
  </Grid>
</Unstyled>

#### Grey gradient

<Unstyled>
  <Grid wrapper spacing={2}>
    <Grid item col={2}>
      <div style={{backgroundColor: variables['grey-90'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
      <Text variant='caption' weight='bold'>$grey-90</Text>
    </Grid>
    <Grid item col={2}>
      <div style={{backgroundColor: variables['grey-80'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
      <Text variant='caption' weight='bold'>$grey-80</Text>
    </Grid>
    <Grid item col={2}>
      <div style={{backgroundColor: variables['grey-70'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
      <Text variant='caption' weight='bold'>$grey-70</Text>
    </Grid>
    <Grid item col={2}>
      <div style={{backgroundColor: variables['grey-60'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
      <Text variant='caption' weight='bold'>$grey-60</Text>
    </Grid>
    <Grid item col={2}>
      <div style={{backgroundColor: variables['grey-50'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
      <Text variant='caption' weight='bold'>$grey-50</Text>
    </Grid>
    <Grid item col={2}>
      <div style={{backgroundColor: variables['grey-40'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
      <Text variant='caption' weight='bold'>$grey-40</Text>
    </Grid>
    <Grid item col={2}>
      <div style={{backgroundColor: variables['grey-30'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
      <Text variant='caption' weight='bold'>$grey-30</Text>
    </Grid>
    <Grid item col={2}>
      <div style={{backgroundColor: variables['grey-20'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
      <Text variant='caption' weight='bold'>$grey-20</Text>
    </Grid>
    <Grid item col={2}>
      <div style={{backgroundColor: variables['grey-10'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
      <Text variant='caption' weight='bold'>$grey-10</Text>
    </Grid>
    <Grid item col={2}>
      <div style={{backgroundColor: variables['grey-5'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
      <Text variant='caption' weight='bold'>$grey-5</Text>
    </Grid>
  </Grid>
</Unstyled>

#### Text

These are the default colours for all texts used within the design system. It is important it stays consistent.

<Unstyled>
  <Grid wrapper spacing={2}>
    <Grid item wrapper spacing={2}>
      <Grid item col={2}>
        <div style={{backgroundColor: variables['text'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
        <Text variant='caption' weight='bold'>$text</Text>
      </Grid>
      <Grid item col={2}>
        <div style={{backgroundColor: variables['text-alt'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
        <Text variant='caption' weight='bold'>$text-alt</Text>
      </Grid>
      <Grid item col={2}>
        <div style={{backgroundColor: variables['text-disabled'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
        <Text variant='caption' weight='bold'>$text-disabled</Text>
      </Grid>
    </Grid>
    <Grid item wrapper spacing={2}>
      <Grid item col={2}>
        <div style={{backgroundColor: variables['text-contrast'], border: '1px solid', borderColor: variables.border, height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
        <Text variant='caption' weight='bold'>$text-contrast</Text>
      </Grid>
      <Grid item col={2}>
        <div style={{backgroundColor: variables['text-contrast-alt'], border: '1px solid', borderColor: variables.border, height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
        <Text variant='caption' weight='bold'>$text-contrast-alt</Text>
      </Grid>
      <Grid item col={2}>
        <div style={{backgroundColor: variables['text-contrast-disabled'], border: '1px solid', borderColor: variables.border, height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
        <Text variant='caption' weight='bold'>$text-contrast-disabled</Text>
      </Grid>
    </Grid>
  </Grid>
</Unstyled>

#### Brand

Pallote uses a maximum of three brand colours. You can of course only use one, or two, for your own branding.

<Unstyled>
  <Grid wrapper spacing={2}>
    <Grid item col={2}>
      <div style={{backgroundColor: variables['primary'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
      <Text variant='caption' weight='bold'>$primary</Text>
    </Grid>
    <Grid item col={2}>
      <div style={{backgroundColor: variables['secondary'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
      <Text variant='caption' weight='bold'>$secondary</Text>
    </Grid>
    <Grid item col={2}>
      <div style={{backgroundColor: variables['highlight'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
      <Text variant='caption' weight='bold'>$highlight</Text>
    </Grid>
  </Grid>
</Unstyled>

#### Misc

<Unstyled>
  <Grid wrapper spacing={2}>
    <Grid item wrapper spacing={2}>
      <Grid item col={2}>
        <div style={{backgroundColor: variables['background'], border: '1px solid', borderColor: variables.border, height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
        <Text variant='caption' weight='bold'>$background</Text>
      </Grid>
      <Grid item col={2}>
        <div style={{backgroundColor: variables['paper'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
        <Text variant='caption' weight='bold'>$paper</Text>
      </Grid>
    </Grid>
    <Grid item wrapper spacing={2}>
      <Grid item col={2}>
        <div style={{backgroundColor: variables['overlay'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
        <Text variant='caption' weight='bold'>$overlay</Text>
      </Grid>
      <Grid item col={2}>
        <div style={{backgroundColor: variables['hover'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
        <Text variant='caption' weight='bold'>$hover</Text>
      </Grid>
      <Grid item col={2}>
        <div style={{backgroundColor: variables['backdrop'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
        <Text variant='caption' weight='bold'>$backdrop</Text>
      </Grid>
    </Grid>
    <Grid item wrapper spacing={2}>
      <Grid item col={2}>
        <div style={{backgroundColor: variables['border'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
        <Text variant='caption' weight='bold'>$border</Text>
      </Grid>
      <Grid item col={2}>
        <div style={{backgroundColor: variables['border-contrast'], border: '1px solid', borderColor: variables.border, height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
        <Text variant='caption' weight='bold'>$border-contrast</Text>
      </Grid>
    </Grid>
  </Grid>
</Unstyled>

#### Feedback

<Unstyled>
  <Grid wrapper spacing={2}>
      <Grid item col={2}>
        <div style={{backgroundColor: variables['success'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
        <Text variant='caption' weight='bold'>$success</Text>
      </Grid>
    <Grid item col={2}>
      <div style={{backgroundColor: variables['info'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
      <Text variant='caption' weight='bold'>$info</Text>
    </Grid>
    <Grid item col={2}>
      <div style={{backgroundColor: variables['warning'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
      <Text variant='caption' weight='bold'>$warning</Text>
    </Grid>
    <Grid item col={2}>
      <div style={{backgroundColor: variables['error'], height: '2.5rem', width: '100%', borderRadius: '0.25rem', marginBottom: '0.25rem'}} className={'p-1'} />
      <Text variant='caption' weight='bold'>$error</Text>
    </Grid>
  </Grid>
</Unstyled>


## Typography

Typography is at the core of every design system. As text is the most present element on screen, it is decisive in how users will perceive your brand. Having consistent and harmonious font styles is paramount.

#### Heading styles

<Unstyled>
  <Table>
    <HeaderRow />
    <HeadingRows />
  </Table>
</Unstyled>

#### Body styles

<Unstyled>
  <Table>
    <HeaderRow />
    <BodyRows />
  </Table>
</Unstyled>

</div>
